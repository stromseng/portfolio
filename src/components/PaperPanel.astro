---
import { cn } from "@/lib/utils";

interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div
  class={cn(
    "rounded-[32px] border-[3px] border-black bg-[#fffdf4] shadow-[12px_12px_0_rgba(0,0,0,0.35)]",
    className,
  )}
>
  <slot />
</div>
